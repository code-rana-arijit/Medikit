apiVersion: apps/v1
kind: Deployment
metadata:
  name: distributor-service
  namespace: medikit
  labels:
    app: distributor-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: distributor-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: distributor-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8114"
    spec:
      priorityClassName: medikit-high
      terminationGracePeriodSeconds: 30
      containers:
        - name: distributor-service
          image: ghcr.io/code-rana-arijit/medikit/distributor-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8114
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: EUREKA_SERVER
              valueFrom:
                configMapKeyRef:
                  name: medikit-common-config
                  key: EUREKA_SERVER
            - name: DB_URL
              value: "jdbc:postgresql://postgres:5432/medikit_distributors"
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: medikit-common-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: medikit-common-config
                  key: DB_PASSWORD
          resources:
            requests:
              cpu: 300m
              memory: 768Mi
            limits:
              cpu: "2"
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8114
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8114
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: distributor-service
  namespace: medikit
  labels:
    app: distributor-service
spec:
  selector:
    app: distributor-service
  ports:
    - port: 8114
      targetPort: 8114
      name: http
