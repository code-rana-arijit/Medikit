# Use Spring Security default security filter chain configuration for Eureka dashboard
spring:
  cloud:
    discovery:
      enabled: false
  security:
    user:
      name: ${EUREKA_USERNAME:admin}
      password: ${EUREKA_PASSWORD:admin}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
